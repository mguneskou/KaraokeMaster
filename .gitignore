## Runtime data (database, separated-audio cache, python venv) — never belongs in source control
## Anchored to repo root only: an unanchored "data/" also matches src/KaraokeMaster.Core/Data/
## (the source folder) on Windows' case-insensitive git, silently dropping it from commits.
/data/

## Personal music library — copyrighted audio, never belongs in source control
music/

## .NET build output
bin/
obj/
publish/

## Visual Studio / Rider / VS Code
.vs/
.vscode/
*.user
*.suo

## OS cruft
Thumbs.db
.DS_Store
